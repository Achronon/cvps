<!-- noema-review-context:begin -->
## Review Memory (Noema)
- Before reviewing changes, read `.noema/review-context.md` if it exists. It carries cross-PR memory for this diff (recurring finding patterns, reviewer learnings, project notes) emitted by `noema review-context`.
- Treat each item as a hint from prior reviews, not a finding; verify against the actual diff.
- When an item informs a finding, cite its trailing `NOEMA-MEM:<id>` marker in the finding text.
<!-- noema-review-context:end -->
